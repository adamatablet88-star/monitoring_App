# תוכנית מימוש — השוואת מסמך ההנחיות (גרסה 2) מול הקוד הקיים

**מסמך זה הוא תוצר של סעיף 20 במסמך ההנחיות: סריקה + השוואה לצ'קליסט
(סעיף 18) בלבד — אין בו שינוי קוד. לא נבנה, לא נערך ולא נמחק דבר
בפרויקט כהכנה למסמך הזה.**

---

## 1. מצב קיים

**סטאק:** Next.js (App Router, static export) + Firebase Realtime
Database (לא Firestore) + Firebase Authentication. אין שרת, אין
Firestore, אין Storage. פרוס כאתר סטטי בנטליפיי.

**מסכי פרוטוטייפ קיימים:** מסך הקמת אתר מלא (`components/admin/*`),
4 טפסי ביקור (`components/field/*`), "מה נדרש החודש"
(`components/compliance/`), דשבורד מגמות (`components/trends/`), ייצוא
Excel (`components/export/`).

**מודלי נתונים קיימים** (`lib/types/*.ts`): `Client`, `Site`, `Well`,
`Tank`, `GroundwaterWell`, `TreatmentSystem`, `TreatmentWell`,
`ParameterConfig`, 4 טיפוסי ביקור (`FuelLensVisit`, `SveSystemVisit`,
`BioVentingSystemVisit`, `GroundwaterVisit`), `FrequencySetting`,
`ActiveStatus`, `RegulatoryReport`, `ScheduledSpecialTest`, `AppUser`.

**הרשאות:** RTDB Security Rules (`database.rules.json`) — admin מול
technician, כולל אכיפת בעלות על ביקורים (24 שעות מרגע היצירה) ובדיקת
תפקיד אמיתית בזמן כתיבה (לא רק ב-UI).

**החלטות שכבר התקבלו בשיחה הזו ולא נפתחות מחדש כאן:**
- **Offline-first מלא (סעיף 12) — הוחלט במפורש לוותר עליו**, כפשרה
  מודעת בהינתן הבחירה ב-Firebase+Netlify בלי שרת. אין תור סנכרון, אין
  אחסון מקומי עמיד במכשיר.
- **קונפליקטים — נבנה פתרון קליל, לא המנגנון המלא מהמסמך.** קיים כיום:
  זיהוי אופטימי (Optimistic Concurrency) שמונע דריסה שקטה של ביקור
  שכבר נשמר, עם הודעת שגיאה לטכנאי השני. **לא** קיים: שמירת שתי הגרסאות
  במקביל, תצוגת diff (מי/מתי/אילו שדות), או יכולת מיזוג/בחירה של מנהל —
  אלה תלויים ארכיטקטונית בתור הסנכרון שהוחלט לוותר עליו.

---

## 2. פערים לפי צ'קליסט (סעיף 18)

### 18.1 קידוח — תעודת זהות
- [x] שם/מזהה, X, Y, Z, שוחה (סוג+מידה), עומק, קוטר, מקטע מחורץ מ-עד
- [ ] **TOC (נקודת ייחוס למדידה) — שדה חסר לחלוטין ב-`WellIdentity`.**
- [ ] **wellType** (treatment/monitoring/groundwater) — קיים ✅
- [ ] **אמצעי פינוי נוכחי (`recoveryMethod`) — פער ארכיטקטוני אמיתי.**
  כרגע זהו שדה **קבוע שהמנהל מגדיר במסך הקמת האתר** (`WellsPanel.tsx`),
  ומשמש לבחירת תת-הטופס בביקור (סקימר/סופח/מיכל). המסמך קובע במפורש
  שזה **לא** שדה קבוע — "נשאב ומתעדכן מדיווחי הטכנאי בשטח, ואינו מוגדר
  במסך הקמת האתר". זו לא תיקון קטן: היא הופכת שדה admin-time לשדה
  field-time, ומשנה את זרימת טופס עדשת הדלק (הטכנאי יצטרך לבחור/לדווח
  את אמצעי הפינוי הנוכחי כחלק מהביקור, במקום שהוא יגיע כבר קבוע מהקידוח).
  **מסומן להחלטה מפורשת — ראו סעיף 4.**

### 18.2 מערכת טיפול
- [x] שדה שם/גודל עם label לפי סוג (`TreatmentSystemsPanel.tsx`)
- [ ] **SVE: קיים Toggle לממיר קטליטי (`hasCatalyticConverter` ב-`SveVisitForm.tsx`) — המסמך אוסר עליו במפורש.** SVE אמורה לכלול ממיר קבוע, ללא בחירה.
- [x] BV: אין שום שדה קטליסט — תקין.
- [ ] **`ParameterConfig` חסר `helpText`** (טולטיפ הסבר לטכנאי) — לא קיים כלל בטיפוס או בטופס הניהול.
- [x] criticalDirection/criticalValue/criticalMessage — קיים ומופרד כראוי.
- [x] טבלת קידוחים אחודה עם `wellType` — קיים.
- [ ] **תמצית 6 חודשים חלקית**: קיימים מינ׳/מקס׳/ממוצע (`FieldHistoryHint`), **חסרים**: מספר מדידות, תאריך מדידה אחרונה, והודעת "אין מספיק נתונים היסטוריים" כשאין מספיק (כרגע לא מוצג כלום).
- [ ] **אין רשימת פרמטרים מוצעת כברירת מחדל** בעת יצירת מערכת SVE/BV חדשה (הרשימות המדויקות בסעיף 5.4 של המסמך) — כרגע `ParametersPanel` מתחיל ריק לגמרי, בלי הצעות.

### 18.3 עדשת דלק — טופס ביקור
- [x] עומק מים/מוצר/עובי מחושב חי, טוגל לא-נמדד + 4 סיבות
- [x] תוכן סקימר (ריק/דלק/דלק+מים, 2 כמויות) + זיהוי אוטומטי עדשה+ריק
- [ ] **רצף השלבים המחייב בסקימר פאסיבי (7.3) אינו מיושם כזרימה מודרכת** — כל השדות קיימים אך שטוחים, לא כ"שלב 1 → שלב 2 → ..." מובנה. פער UX, לא פער נתונים.
- [ ] **סופח: חסרים תאריך החלפה וסיבת החלפה** — קיים היום רק `condition`+`replaced` (בוליאני).
- [ ] **מבנה פינוי חסר שדה `notes`** לכל פעולת פינוי (יש רק method+liters).
- [ ] **אין מימוש בפועל לחישוב הצבירה במיכל המשותף (סעיף 8)** — נשמרים `currentVolume`+`emptiedSincePrevious` לכל ביקור, אבל **אין שום מקום באפליקציה שמחשב/מציג את הסכום המצטבר** לאורך זמן עם איפוס. זו יכולת שלמה שלא נבנתה, לא רק שדה חסר.
- [ ] **תמונות (אופציונלי)** — לא קיים כלל שום מנגנון העלאת תמונה באפליקציה (אין Firebase Storage — החלטה מהפרויקט המקורי). **דורש החלטה, ראו סעיף 4.**

### 18.4 SVE — טופס ביקור
- [x] סוג ביקור קטן/גדול/Baseline, מצב הגעה + ניסיון הפעלה + דגל תקלה
- [ ] **אין אזהרה כששעות עבודה יורדות מהביקור הקודם, ואין שדה הסבר שנפתח אוטומטית.**
- [x] % פתיחה + "הכל ללא שינוי" (גלוי וניתן לעריכה)
- [x] וואקום סעפת + וואקום מפריד לחות, שניהם שליליים באחסון; VCV
- [ ] **PID לפני/אחרי וממיר קטליטי בנויים כשדות קבועים ב-schema, לא כפרמטרים גמישים** — המסמך (5.4) מרמז שאלה אמורים להיות חלק ממנוע הפרמטרים הדינמי (ניתנים לעריכה/מחיקה/הוספה על ידי מנהל), לא שדות קשיחים. זו שאלה ארכיטקטונית משמעותית — **ראו סעיף 4**.
- [x] טיפול גדול: טופס נוסף לכל קידוח, לא הכל חובה

### 18.5 Bio-venting — טופס ביקור
- [x] אין הבחנה קטן/גדול; מצב הגעה + פילטר (3 מצבים) + דגל
- [x] וואקום קו יניקה שלילי; % פתיחה + "הכל ללא שינוי"; ברז דילול באחוזים
- [x] נקודות ניטור עם מספר עומקים גמיש; לכל עומק O2/CO2/CH4/PID/וואקום
- [ ] **מבחן חמצן שנתי חסר שדה הערה** (`annualOxygenTest` יש רק `done`+`date`).
- [ ] **וואקום קו יניקה / O2 / CO2 קבועים ב-schema, לא בפרמטרים גמישים** — אותה שאלה ארכיטקטונית כמו ב-SVE.

### 18.6 דיגום מי תהום — טופס ביקור
**כל הפריטים בסעיף זה מיושמים.** תקינות פקק/צינור, עדשה, נפח מים מחושב
אוטומטית, עומק דיגום מוצע ניתן לעריכה, לוג ייצוב עם קריטריונים מדויקים
(3 קריאות רצופות, לא חוסם), בחירת בדיקות גמישה עם כלי דיגום אוטומטי.
**אין** תמונות (אותה החלטת Storage כמו בסעיף 18.3).

### 18.7 מסך הקמת אתר (Admin)
- [ ] **תווית "לקוחות (חברות דלק)" עדיין מכילה את הניסוח שהמסמך מבקש להסיר** — צריך "שם לקוח" נטו.
- [ ] **אין הפרדה בין כתובת (עם hint ל-Waze) לבין נ.צ מרכזי** — קיים היום שדה `location` יחיד ומעורפל (`{lat,lng} | string`). זו תוספת שדה למודל `Site`, לא רק לטופס.
- [x] 4 טוגלים לפרוטוקולים כולל דיגום מי תהום — תקין.
- [ ] **טבלת קידוחי עדשת דלק כוללת עמודת/שדה אמצעי שאיבה בטופס היצירה** — צריכה להיעלם משם (ראו 18.1).
- [x] מערכות עם label לפי סוג — תקין (אך ראו הפער הארכיטקטוני של הפרמטרים הקבועים למעלה).
- [ ] **אין שורת תדירות נפרדת לפרוטוקול "דיגום מי תהום" ברמת אתר** — `FrequencySettingsPanel`/`ActiveStatusPanel` כרגע מכסים רק "האתר (עדשת דלק)" ברמת site scope, ומערכת טיפול ברמת system scope. אתר עם פרוטוקול מי-תהום עצמאי (בלי מערכת טיפול) אין לו שורת תדירות/סטטוס משלו.

### 18.8 כלל-מערכתי
- [x] Soft Warning לא חוסם שמירה בשום מקום שנבדק.
- [x] Audit Trail על **שינוי תדירות** — קיים (`FrequencySetting.history`).
- [ ] **אין Audit Trail על שינוי פרמטר** (`ParameterConfig` — אין שדה history כלל).
- [ ] **אין Audit Trail על שינוי מבנה קידוח/מערכת** (יצירה/עריכה/מחיקה של Well/TreatmentSystem/Site — נכתב ישירות בלי תיעוד שינויים).
- [x] הרשאות: טכנאי לא יכול להוסיף משתמש/לשנות מבנה/לערוך נתון ישן של אחר — נאכף ברמת RTDB Rules, אומת בפועל מול emulator.
- [x] "מה נדרש החודש" נגיש לטכנאים (לא רק למנהל), ואינו בונה סידור אוטומטי.

---

## 3. פערים נוספים שלא מכוסים במפורש בסעיף 18 אך נדרשים בגוף המסמך

- **`Client` הוא stub של `{id, name}` בלבד.** המסמך (4.1) דורש: אנשי
  קשר, פרוטוקולי ניטור רלוונטיים, תדירויות ברירת מחדל לפי חוזה, הגדרות
  דוחות, תאריכי יצירה/עדכון. שום דבר מזה לא קיים חוץ מהשם.
- **`Site` חסר: סטטוס אתר, הערות, תאריכי יצירה/עדכון**, בנוסף לפער
  הכתובת/נ.צ שכבר צוין למעלה.
- **סעיף 16 (בדיקות חובה): לא קיים אף קובץ בדיקה אחד בפרויקט** —
  אין Jest/Vitest/כל framework, ואין test script ב-`package.json`.
  כל 16 סעיפי הבדיקות ברשימה (חישוב עובי עדשה, נפח באר, יעילות SVE,
  המרת וואקום, ייצוב, צבירת מיכל, הרשאות, ואלידציה רכה וכו') לא מכוסים
  אוטומטית בשום מקום.
- **ייצוא Excel: תאים חסרים לא מומרים ל-"-"** — כרגע ערכי `null`/`undefined`
  פשוט מוצגים ריקים, לא כטקסט "-" כמבוקש.
- **סעיף 6.1 (שמירת ערכי מדידה)**: המסמך מפרט שדות ברמת כל מדידה בודדת
  (מקור נתון, מספר ביקור, סטטוס סנכרון). כרגע המידע הזה נשמר ברמת
  **הביקור כולו**, לא לכל שדה בנפרד — פישוט סביר בהינתן שאין תור
  סנכרון, אבל שווה לתעד כסטייה מכוונת אם ממשיכים כך.

---

## 4. החלטות שדורשות אישור לפני המשך

1. **אמצעי פינוי (`recoveryMethod`) — admin-defined קבוע (כמו היום) או
   field-derived (כמו שהמסמך דורש)?** זו לא תוספת — היא משנה את זרימת
   טופס עדשת הדלק ואת מסך הקמת האתר. אם עוברים לגרסת המסמך: מסך הקמת
   אתר מפסיק לשאול על אמצעי פינוי; טופס הביקור מוסיף שדה "אמצעי פינוי
   נוכחי" שהטכנאי בוחר/מעדכן בכל ביקור; הערך "האחרון שדווח" הופך להיות
   מה שקובע איזה תת-טופס (סקימר/סופח/מיכל) מוצג.
2. **PID/וואקום/ממיר-קטליטי ב-SVE, ו-O2/CO2/וואקום ב-Bio-venting —
   שדות קבועים ב-schema (כמו היום) או פרמטרים במנוע הגמיש
   (`ParameterConfig`/`ParameterReading`) עם ברירות מחדל מוצעות בעת
   יצירת מערכת?** זו החלטה משמעותית: היא תשנה את `SveSystemVisit`/
   `BioVentingSystemVisit` (פחות שדות קבועים), את דשבורד המגמות (שקורא
   כרגע שדות קבועים כמו `pidAfterConverter` לפי שם), ואת ייצוא ה-Excel.
   הביטול של ה-toggle לממיר קטליטי ב-SVE (סעיף 18.2) קשור ישירות
   להחלטה הזו.
3. **תמונות בטפסי שטח (עדשת דלק, דיגום מי תהום)** — המסמך המקורי
   שהניע את הפרויקט קבע במפורש "אין Firebase Storage". המסמך הנוכחי
   מבקש תמונות אופציונליות. יש צורך בהחלטה: לוותר על תמונות (כמו
   שהוחלט על אופליין), או לאתר פתרון אחסון תמונות שלא Firebase Storage
   (למשל URL חיצוני שהטכנאי מדביק, לא רלוונטי בפועל בשטח) — לא נראה
   פתרון סביר בלי Storage אמיתי.
4. **עומק צבירת המיכל (סעיף 8)** — האם להציג את החישוב המצטבר כמסך/
   דוח נפרד, כשורה בדשבורד המגמות, או כטבלה בייצוא ה-Excel (שכבר יש בו
   "טבלה 3 — סקימר אקטיבי")? זו בחירת מיקום UI, לא רק לוגיקה.
5. **היקף הבדיקות האוטומטיות (סעיף 16)** — האם להקים תשתית בדיקות
   (למשל Vitest) בפרויקט הזה עכשיו, כולל כל 16 הסעיפים, או להתמקד
   קודם בסגירת פערי הנתונים/UI ולהשאיר בדיקות לשלב נפרד?

---

## 5. סדר מימוש מומלץ (לאחר קבלת החלטות סעיף 4)

1. תוספות מודל נתונים "בטוחות" (לא תלויות בהחלטה ארכיטקטונית): TOC
   לקידוח, שדות Client/Site חסרים (כתובת+נ.צ נפרדים, סטטוס, הערות,
   contacts), תאריך/סיבת החלפה לסופח, `notes` לפעולת פינוי, הערה
   למבחן חמצן שנתי, `helpText` ל-`ParameterConfig`.
2. יישום ההחלטה על `recoveryMethod` (אחת מהשתיים בסעיף 4.1) — משפיע על
   מסך הקמת אתר וטופס עדשת דלק גם יחד.
3. יישום ההחלטה על שדות קבועים מול מנוע פרמטרים גמיש ב-SVE/BV (4.2) —
   כולל הסרת ה-toggle לממיר קטליטי, וברירות המחדל המוצעות ליצירת מערכת.
4. תמצית 6 חודשים מלאה (מספר מדידות + תאריך אחרון + הודעת "אין מספיק
   נתונים").
5. Audit Trail לשינוי פרמטר ולשינוי מבנה קידוח/מערכת.
6. שורת תדירות/סטטוס עצמאית לפרוטוקול דיגום מי תהום ברמת אתר.
7. חישוב והצגת צבירת המיכל המשותף (לפי ההחלטה במיקום ה-UI, סעיף 4.4).
8. תיקוני טקסט/UX קטנים: תווית "שם לקוח", hint ל-Waze, "-" לתאים
   חסרים בייצוא Excel, זרימת שלבים מודרכת לסקימר פאסיבי.
9. תמונות — רק אם התקבלה החלטה בסעיף 4.3 עם פתרון אחסון ריאלי.
10. תשתית בדיקות אוטומטיות (אם הוחלט בסעיף 4.5).

---

## 6. סיכונים טכניים

- **שינוי `recoveryMethod` מ-admin ל-field-derived** עלול לשבור נתונים
  קיימים אם כבר נוצרו קידוחים אמיתיים במערכת (יש למפות/למגר נתונים
  קיימים, לא רק לשנות סכימה קדימה).
- **מעבר שדות SVE/BV קבועים למנוע הפרמטרים הגמיש** משפיע על 3 מקומות
  בו-זמנית (טופס ביקור, דשבורד מגמות, ייצוא Excel) — סיכון לרגרסיה
  בשלושתם אם לא נעשה בזהירות ובבדיקה מלאה בכל שלב.
- **אין רשת/גישה ל-Firebase מהסביבה הזו** (מגבלה קיימת וידועה כל
  השיחה) — כל שינוי בכללי RTDB או בזרימות שדורשות Auth אמיתי ימשיך
  להיבדק מול Database Emulator מקומי בלבד, לא מול הפרויקט האמיתי.

---

**לפי הנחיית סעיף 20 במסמך: לא יבוצע מימוש נוסף עד לקבלת הכרעה על
סעיף 4 (החלטות הדורשות אישור) ואישור על סדר המימוש בסעיף 5.**
