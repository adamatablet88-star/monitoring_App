# תוכנית מימוש — השוואת מסמך ההנחיות (גרסה 2) מול הקוד הקיים

**מסמך זה הופק במקור כתוצר של סעיף 20 (סריקה + השוואה לצ'קליסט, ללא
שינוי קוד). לאחר קבלת ההחלטות בסעיף 4 המקורי, בוצע המימוש בפועל — סעיף
2 עודכן למטה כדי לשקף אילו פערים נסגרו.**

---

## 1. מצב קיים

**סטאק:** Next.js (App Router, static export) + Firebase Realtime
Database (לא Firestore) + Firebase Authentication. אין שרת, אין
Firestore, אין Storage. פרוס כאתר סטטי בנטליפיי.

**מסכי פרוטוטייפ קיימים:** מסך הקמת אתר מלא (`components/admin/*`),
4 טפסי ביקור (`components/field/*`), "מה נדרש החודש"
(`components/compliance/`), דשבורד מגמות (`components/trends/`), ייצוא
Excel (`components/export/`).

**מודלי נתונים קיימים** (`lib/types/*.ts`): `Client`, `Site`, `Well`,
`Tank`, `GroundwaterWell`, `TreatmentSystem`, `TreatmentWell`,
`ParameterConfig`, 4 טיפוסי ביקור (`FuelLensVisit`, `SveSystemVisit`,
`BioVentingSystemVisit`, `GroundwaterVisit`), `FrequencySetting`,
`ActiveStatus`, `RegulatoryReport`, `ScheduledSpecialTest`, `AppUser`.

**הרשאות:** RTDB Security Rules (`database.rules.json`) — admin מול
technician, כולל אכיפת בעלות על ביקורים (24 שעות מרגע היצירה) ובדיקת
תפקיד אמיתית בזמן כתיבה (לא רק ב-UI).

**החלטות שכבר התקבלו בשיחה הזו ולא נפתחות מחדש כאן:**
- **Offline-first מלא (סעיף 12) — הוחלט במפורש לוותר עליו**, כפשרה
  מודעת בהינתן הבחירה ב-Firebase+Netlify בלי שרת. אין תור סנכרון, אין
  אחסון מקומי עמיד במכשיר.
- **קונפליקטים — נבנה פתרון קליל, לא המנגנון המלא מהמסמך.** קיים כיום:
  זיהוי אופטימי (Optimistic Concurrency) שמונע דריסה שקטה של ביקור
  שכבר נשמר, עם הודעת שגיאה לטכנאי השני. **לא** קיים: שמירת שתי הגרסאות
  במקביל, תצוגת diff (מי/מתי/אילו שדות), או יכולת מיזוג/בחירה של מנהל —
  אלה תלויים ארכיטקטונית בתור הסנכרון שהוחלט לוותר עליו.

**4 ההחלטות הארכיטקטוניות שבסעיף 4 (למטה) התקבלו ומומשו:**
1. **`recoveryMethod`** → **field-derived** (הוחלט; מומש). מסך הקמת האתר
   כבר לא שואל על אמצעי פינוי/מיכל; טופס עדשת הדלק מוסיף שדה "אמצעי
   פינוי נוכחי" (+ בחירת מיכל כשרלוונטי) שהטכנאי מדווח/מעדכן בכל ביקור;
   הערך "האחרון שדווח" נכתב חזרה כמטמון על ה-`Well` (למסכי ניהול/ייצוא
   שצריכים "מצב נוכחי" בלי לסרוק כל ביקור), אבל אינו עוד מקור האמת.
2. **PID/וואקום/ממיר-קטליטי (SVE) ו-O2/CO2/וואקום (Bio-venting)** →
   **הועברו למנוע הפרמטרים הגמיש** (הוחלט; מומש). `ParameterConfig`
   הורחב עם `helpText`/`active`/`invertSign`; יצירת מערכת SVE/BV חדשה
   זורעת אוטומטית שורות ברירת-מחדל (`lib/defaultParameters.ts`); ה-toggle
   לממיר קטליטי ב-SVE הוסר לגמרי (הממיר תמיד קיים, כמו במסמך); דשבורד
   המגמות וייצוא ה-Excel עודכנו לקרוא מ-`extraReadings` במקום משדות קבועים.
3. **תמונות בטפסי שטח** → **בוטלו** (הוחלט; אין שינוי קוד נדרש — הן
   מעולם לא היו קיימות). אין Firebase Storage בסטאק הזה, כפי שהוחלט
   כבר בשלב הפרויקט המקורי.
4. **היקף בדיקות אוטומטיות (סעיף 16)** → **נדחה לשלב נפרד**, אחרי סגירת
   פערי הנתונים/UI. **מומש**: לאחר שכל פערי סעיף 5 נסגרו, הוקמה תשתית
   Vitest (`npm run test`) עם 47 בדיקות יחידה על הלוגיקה הטהורה
   (עובי עדשה, נפח באר, יעילות SVE, המרת סימן וואקום, ולידציה רכה,
   צבירת מיכל, ייצוב מי תהום, סטטוס "מה נדרש החודש") — ראו סעיף 3
   ("סעיף 16") לפירוט מלא ולמה שנשאר לא מכוסה (בעיקר: RTDB Rules,
   שממשיכות להיבדק ידנית מול Emulator כפי שתועד מההתחלה, ולא זרימות UI
   מלאות).

---

## 2. פערים לפי צ'קליסט (סעיף 18)

### 18.1 קידוח — תעודת זהות
- [x] שם/מזהה, X, Y, Z, שוחה (סוג+מידה), עומק, קוטר, מקטע מחורץ מ-עד
- [x] **TOC (נקודת ייחוס למדידה)** — נוסף ל-`WellIdentity` (`toc: number`), משותף לכל שלושת סוגי הקידוח (עדשת דלק/מערכת טיפול/מי תהום) דרך `IdentityFields.tsx`/`identityForm.ts`.
- [x] **wellType** (treatment/monitoring/groundwater) — קיים (`TreatmentWell.wellType`).
- [x] **אמצעי פינוי נוכחי (`recoveryMethod`) — הוסב ל-field-derived.**
  מסך הקמת האתר (`WellsPanel.tsx`) כבר לא שואל על אמצעי פינוי/מיכל;
  `FuelLensVisitForm.tsx` מוסיף שדה "אמצעי פינוי נוכחי" (+ מיכל כשפעיל)
  שהטכנאי מדווח/מעדכן בכל ביקור ונשמר על גבי הביקור עצמו; הערך נכתב
  חזרה כמטמון על ה-`Well` לתצוגה/ייצוא בלבד.

### 18.2 מערכת טיפול
- [x] שדה שם/גודל עם label לפי סוג (`TreatmentSystemsPanel.tsx`)
- [x] **SVE: ה-Toggle לממיר קטליטי הוסר** — הממיר תמיד קיים (קבוע), ללא בחירה; הודעת מידע קבועה מוצגת בטופס יצירת המערכת.
- [x] BV: אין שום שדה קטליסט — תקין.
- [x] **`ParameterConfig` מכיל `helpText`** — טולטיפ + טקסט גלוי לטכנאי, גם בטופס הניהול וגם בטופס השטח.
- [x] criticalDirection/criticalValue/criticalMessage — קיים ומופרד כראוי.
- [x] טבלת קידוחים אחודה עם `wellType` — קיים.
- [x] **תמצית 6 חודשים מלאה**: מינ׳/מקס׳/ממוצע/מספר מדידות/תאריך אחרון, והודעת "אין מספיק נתונים היסטוריים" כשאין די נתונים.
- [x] **רשימת פרמטרים מוצעת כברירת מחדל** נזרעת אוטומטית ביצירת מערכת SVE/BV חדשה (`lib/defaultParameters.ts`, לפי סעיף 5.4 של המסמך).

### 18.3 עדשת דלק — טופס ביקור
- [x] עומק מים/מוצר/עובי מחושב חי, טוגל לא-נמדד + 4 סיבות
- [x] תוכן סקימר (ריק/דלק/דלק+מים, 2 כמויות) + זיהוי אוטומטי עדשה+ריק
- [x] **רצף השלבים בסקימר פאסיבי (7.3) מיושם כזרימה מודרכת** — שדות מקובצים תחת "שלב 1/2/3" עם חשיפה מדורגת, ובחירת סיבה (כש-showAutoSuggestion) הפכה לחובה בפועל בשמירה (לא רק הצעה) — חסימת שמירה עם הודעת שגיאה אם לא נבחרה.
- [x] **סופח: תאריך החלפה וסיבת החלפה** — `absorbentCheck.replacedDate`/`replacedReason`, מוצגים בטופס כשה-"הוחלף" מסומן.
- [x] **מבנה פינוי כולל שדה `notes`** אופציונלי לכל פעולת פינוי.
- [x] **חישוב הצבירה במיכל המשותף (סעיף 8)** — `lib/tankAccumulation.ts`'s `computeTankAccumulation` מחשב סה"כ נאסף לאורך זמן (סכימת הקריאה שקדמה לכל אירוע "רוקן"), מוצג הן בטבלה 3 של ייצוא ה-Excel והן כטבלת "צבירת מיכלים משותפים" חדשה בדשבורד המגמות (לפי החלטת המשתמש: שני המקומות האלה, לא מסך ייעודי נפרד). אומת בסקריפט עצמאי מול תרחיש עם שני אירועי ריקון.
- [x] **תמונות (אופציונלי)** — **הוחלט לוותר** (אין Firebase Storage בסטאק הזה — החלטה מהפרויקט המקורי, אושרה מחדש). אין מנגנון העלאת תמונה, ולא ייבנה.

### 18.4 SVE — טופס ביקור
- [x] סוג ביקור קטן/גדול/Baseline, מצב הגעה + ניסיון הפעלה + דגל תקלה
- [x] **אזהרה כששעות עבודה יורדות מהביקור הקודם + שדה הסבר שנפתח אוטומטית** — הוכלל למנוע הפרמטרים הגמיש כדגל `monotonicIncreasing` כללי (כמו `invertSign`), לא מיוחד ל-SVE בלבד; `ParameterReading` קיבל `note` אופציונלי לכך.
- [x] % פתיחה + "הכל ללא שינוי" (גלוי וניתן לעריכה)
- [x] וואקום סעפת + וואקום מפריד לחות, שניהם שליליים באחסון; VCV
- [x] **וואקום סעפת/מפריד-לחות, VCV, לחץ ממיר קטליטי, PID לפני/אחרי, ושעות עבודה הועברו למנוע הפרמטרים הגמיש** (`extraReadings`, נזרעים כברירת מחדל — `SVE_PARAMETER_KEYS`). `efficiencyPercent` נשאר שדה מחושב-ונשמר על הביקור (כדי שדשבורד המגמות וייצוא ה-Excel לא יצטרכו לחשב מחדש), אבל נגזר מ-2 קריאות ה-PID שכבר חיות כפרמטרים.
- [x] טיפול גדול: טופס נוסף לכל קידוח, לא הכל חובה

### 18.5 Bio-venting — טופס ביקור
- [x] אין הבחנה קטן/גדול; מצב הגעה + פילטר (3 מצבים) + דגל
- [x] וואקום קו יניקה שלילי; % פתיחה + "הכל ללא שינוי"; ברז דילול באחוזים
- [x] נקודות ניטור עם מספר עומקים גמיש; לכל עומק O2/CO2/CH4/PID/וואקום
- [x] **מבחן חמצן שנתי כולל שדה הערה** (`annualOxygenTest.note`, אופציונלי).
- [x] **וואקום קו יניקה / O2 / CO2 (ברמת המערכת) הועברו למנוע הפרמטרים הגמיש** (`extraReadings`, נזרעים כברירת מחדל — `BIO_VENTING_PARAMETER_KEYS`). ה-O2/CO2 לכל עומק בכל נקודת ניטור נשארו כשהיו — אלה שדות שונים, לא כפילות.

### 18.6 דיגום מי תהום — טופס ביקור
**כל הפריטים בסעיף זה מיושמים.** תקינות פקק/צינור, עדשה, נפח מים מחושב
אוטומטית, עומק דיגום מוצע ניתן לעריכה, לוג ייצוב עם קריטריונים מדויקים
(3 קריאות רצופות, לא חוסם), בחירת בדיקות גמישה עם כלי דיגום אוטומטי.
**אין** תמונות (אותה החלטת Storage כמו בסעיף 18.3).

### 18.7 מסך הקמת אתר (Admin)
- [x] **תווית "לקוחות (חברות דלק)" נוקתה** ל-"לקוחות" נטו.
- [x] **הפרדה בין כתובת (עם hint ל-Waze) לבין נ.צ מרכזי** — `Site.location` המעורפל הוחלף ב-`address: string` (עם placeholder המפנה לניווט) ו-`coordinates: {lat,lng}` נפרדים.
- [x] 4 טוגלים לפרוטוקולים כולל דיגום מי תהום — תקין.
- [x] **שדה אמצעי שאיבה בטופס יצירת קידוח עדשת דלק הוסר** (ראו 18.1) — מוחלף בהערת הבהרה שהערך נקבע בשטח.
- [x] מערכות עם label לפי סוג — תקין; שדות הפרמטרים הקבועים לשעבר כבר עברו למנוע הגמיש (ראו 18.4/18.5).
- [x] **שורת תדירות/סטטוס נפרדת לפרוטוקול "דיגום מי תהום" ברמת אתר** — `FrequencyScope` הפך למאובחן (`{siteId, protocol}` עם `protocol: "fuelLens" | "groundwater"`, בנוסף ל-`{systemId}`); `FrequencySettingsPanel`/`ActiveStatusPanel` מציגים שורת "האתר (דיגום מי תהום)" נפרדת כשה-`Site.protocolTypes` כולל `groundwater`, ו-"מה נדרש החודש" (`DueThisMonthView`) מוסיף שורה מקבילה לכל אתר כזה לפי הביקור האחרון מ-`groundwaterVisits`.

### 18.8 כלל-מערכתי
- [x] Soft Warning לא חוסם שמירה בשום מקום שנבדק.
- [x] Audit Trail על **שינוי תדירות** — קיים (`FrequencySetting.history`).
- [x] **Audit Trail על שינוי פרמטר** — `ParameterConfig.history` (בדיוק כמו `FrequencySetting.history`): רשומה בכל יצירה/הפעלה-מחדש/השבתה, מוצגת inline בכל שורת פרמטר.
- [x] **Audit Trail על שינוי מבנה קידוח/מערכת/אתר** — יומן נפרד (`structureAuditLog`, append-only ברמת ה-RTDB Rules — אומת מול Emulator שגם admin לא יכול לערוך/למחוק רשומה קיימת) מתעד יצירה/מחיקה של Well/TreatmentSystem/Site, כי בניגוד ל-`FrequencySetting` רשומה שנמחקת לא משאירה שורה משלה לצרף אליה history. מוצג כ-`<details>` בתחתית כל אחד מ-`WellsPanel`/`TreatmentSystemsPanel`/`SitesPanel`. **עריכה** של הישויות האלה עדיין לא קיימת באפליקציה בכלל (רק יצירה/מחיקה) — כשתתווסף, תצטרך להצטרף ליומן הזה.
- [x] הרשאות: טכנאי לא יכול להוסיף משתמש/לשנות מבנה/לערוך נתון ישן של אחר — נאכף ברמת RTDB Rules, אומת בפועל מול emulator.
- [x] "מה נדרש החודש" נגיש לטכנאים (לא רק למנהל), ואינו בונה סידור אוטומטי.

---

## 3. פערים נוספים שלא מכוסים במפורש בסעיף 18 אך נדרשים בגוף המסמך

- [x] **`Client` הורחב מ-stub `{id, name}`** ל: `contacts` (רשימת אנשי
  קשר עם שם/תפקיד/טלפון/דוא"ל), `relevantProtocolTypes` (פרוטוקולי
  ניטור רלוונטיים לפי חוזה), `defaultFrequency` (תדירות ברירת מחדל לפי
  חוזה — נטענת בפועל כברירת מחדל ב-`FrequencySettingsPanel` כשאין עדיין
  `FrequencySetting` מפורש), `reportNotes` (הגדרות/הערות חופשיות
  לדוחות), `createdAt`/`updatedAt`.
- [x] **`Site` הורחב עם: `status`** (פעיל/לא פעיל, מוצג ברשימת האתרים),
  **`notes`, `createdAt`/`updatedAt`**, בנוסף לפיצול הכתובת/נ.צ שכבר
  צוין למעלה (18.7).
- [x] **סעיף 16 (בדיקות חובה)** — Vitest (`npm run test`), 47 בדיקות
  ב-8 קבצים. מכוסה: חישוב עובי עדשה (`lib/fuelLens.ts`), נפח באר
  (`lib/wellVolume.ts`), יעילות SVE (`lib/sveEfficiency.ts`), המרת סימן
  וואקום + ולידציה רכה (`lib/parameterMath.ts`), צבירת מיכל
  (`lib/tankAccumulation.ts`, כבר היה קיים מסעיף 8), ייצוב מי תהום
  (`components/field/groundwater/stabilization.ts`, כבר היה קיים),
  סטטוס "מה נדרש החודש" (`components/compliance/dueStatus.ts`, כבר
  היה קיים) והקשר היסטורי (`components/field/fieldHistory.ts`, כבר
  היה קיים). כל פונקציית חישוב שהייתה מוטבעת בתוך קומפוננטת טופס הוצאה
  ל-`lib/*.ts` עצמאי כדי לאפשר את זה. **לא מכוסה**: **הרשאות** (RTDB
  Rules) — ממשיכות להיבדק ידנית מול Database Emulator אמיתי כפי שתועד
  לאורך כל הפרויקט (Vitest לא יכול להריץ Java emulator), וזרימות UI
  מלאות (אין React Testing Library/Playwright).
- [x] **ייצוא Excel: תאים חסרים מומרים ל-"-"** — `cellOrDash` בטבלה 2 ובנספח ההיסטוריה (עומק מים/מוצר/עובי עדשה, כמויות פינוי).
- **סעיף 6.1 (שמירת ערכי מדידה)**: המסמך מפרט שדות ברמת כל מדידה בודדת
  (מקור נתון, מספר ביקור, סטטוס סנכרון). כרגע המידע הזה נשמר ברמת
  **הביקור כולו**, לא לכל שדה בנפרד — פישוט סביר בהינתן שאין תור
  סנכרון, אבל שווה לתעד כסטייה מכוונת אם ממשיכים כך.

---

## 4. החלטות (התקבלו — ראו סעיף 1) ומה שעדיין פתוח

**התקבלו ומומשו (1, 2, 3) או התקבלו ונדחו במודע לשלב נפרד (5):**

1. ~~אמצעי פינוי (`recoveryMethod`) — admin-defined או field-derived?~~
   **הוחלט: field-derived. מומש.**
2. ~~PID/וואקום/ממיר-קטליטי ב-SVE, ו-O2/CO2/וואקום ב-Bio-venting —
   שדות קבועים או מנוע פרמטרים גמיש?~~ **הוחלט: מנוע גמיש. מומש.**
3. ~~תמונות בטפסי שטח?~~ **הוחלט: לוותר. אין שינוי קוד — לא נבנה.**
4. ~~עומק צבירת המיכל (סעיף 8) — מסך/דוח נפרד, שורה בדשבורד המגמות,
   או טבלה בייצוא ה-Excel?~~ **הוחלט: גם בטבלה 3 של ייצוא ה-Excel וגם
   כטבלה חדשה בדשבורד המגמות (לא מסך נפרד). מומש.**
5. ~~היקף הבדיקות האוטומטיות (סעיף 16)?~~ **הוחלט: שלב נפרד, אחרי סגירת
   פערי הנתונים/UI. מומש — ראו סעיף 3 ("סעיף 16").**

---

## 5. סדר מימוש — מה שבוצע ומה שנשאר

**בוצע (שלבים 2-3 המקוריים, כולל אימות tsc/lint/build מלא בכל שלב):**
- ~~2. יישום ההחלטה על `recoveryMethod`~~ — מומש (`WellsPanel.tsx`,
  `FuelLensVisitForm.tsx`, `lib/types/well.ts`, `lib/types/visits.ts`).
- ~~3. יישום ההחלטה על שדות קבועים מול מנוע פרמטרים גמיש ב-SVE/BV~~ —
  מומש (`lib/defaultParameters.ts`, `ParameterConfig` הורחב, הוסר
  ה-toggle לממיר קטליטי, `SveVisitForm.tsx`/`BioVentingVisitForm.tsx`/
  `sveTrends.ts`/`bioVentingTrends.ts` עודכנו).
- ~~4. (חלק) תמצית 6 חודשים מלאה~~ — מומש (`fieldHistory.ts`,
  `FieldHistoryHint.tsx`).
- הערה: `helpText` ל-`ParameterConfig` (משלב 1 המקורי) מומש כחלק משלב 3.

**בוצע גם:**
- ~~TOC לקידוח~~ — מומש (`WellIdentity.toc`, משותף לכל 3 סוגי הקידוח).
- ~~שדות Client/Site חסרים~~ — מומש: `Client` קיבל `contacts`,
  `relevantProtocolTypes`, `defaultFrequency` (מוזן בפועל כברירת מחדל
  ב-`FrequencySettingsPanel`), `reportNotes`, `createdAt`/`updatedAt`;
  `Site` קיבל `address`+`coordinates` נפרדים (במקום `location` המעורפל),
  `status` (מוצג ברשימת האתרים), `notes`, `createdAt`/`updatedAt`.
  שדה "תאריך/סיבת החלפה לסופח" ו-`notes` לפעולת פינוי (18.3) מומשו
  בנפרד, ראו שורה למטה.
- ~~תאריך/סיבת החלפה לסופח, `notes` לפעולת פינוי~~ — מומש (18.3).
- ~~Audit Trail לשינוי פרמטר ולשינוי מבנה קידוח/מערכת~~ — מומש:
  `ParameterConfig.history` (inline, כמו `FrequencySetting`) +
  `structureAuditLog` (יומן נפרד, append-only, לתיעוד יצירה/מחיקה של
  Well/TreatmentSystem/Site). אומת מול Database Emulator אמיתי: admin
  יכול ליצור רשומת יומן אך לא לערוך/למחוק רשומה קיימת; טכנאי לא יכול
  לכתוב ליומן בכלל.
- ~~שורת תדירות/סטטוס עצמאית לפרוטוקול דיגום מי תהום ברמת אתר~~ — מומש:
  `FrequencyScope` הפך למאובחן לפי פרוטוקול (`fuelLens`/`groundwater`)
  ברמת אתר, בנוסף ל-`{systemId}`; `FrequencySettingsPanel`/
  `ActiveStatusPanel`/`DueThisMonthView` מוסיפים שורה נפרדת לכל אתר עם
  פרוטוקול מי-תהום.
- ~~חישוב והצגת צבירת המיכל המשותף~~ — מומש: `lib/tankAccumulation.ts`,
  מוצג בטבלה 3 של ייצוא ה-Excel וכטבלה חדשה בדשבורד המגמות.
- ~~תיקוני טקסט/UX קטנים~~ — כולם מומשו: תווית "לקוחות" (במקום "לקוחות
  (חברות דלק)"), "-" לתאים חסרים בייצוא ה-Excel, זרימת שלבים מודרכת
  ("שלב 1/2/3") לבדיקת סקימר פאסיבי עם בחירת סיבה מחייבת בפועל, ואזהרת
  ירידת ערך + שדה הסבר אוטומטי למדדים "מצטברים" (דגל `monotonicIncreasing`
  כללי במנוע הפרמטרים, לא רק ל-SVE).
- ~~תשתית בדיקות אוטומטיות~~ — מומש: Vitest, `npm run test`, 47 בדיקות
  ב-8 קבצים על הלוגיקה הטהורה. ראו סעיף 3 ("סעיף 16") לפירוט מלא.

**כל פריטי רשימת סעיף 5 המקורית נסגרו.** ראה סעיף 3 להיקף המדויק של
כיסוי הבדיקות (מה כן ומה לא מכוסה).

---

## 6. סיכונים טכניים

- **שינוי `recoveryMethod` מ-admin ל-field-derived** — **בוצע על סכימה
  קדימה בלבד**, כפי שהוזהר. אין נתונים אמיתיים היום בפרויקט (אין גישת
  רשת ל-Firebase האמיתי מהסביבה הזו), כך שאין נתונים היסטוריים למגר;
  אם וכאשר יעלה נתון אמיתי, `Well.recoveryMethod` הקיים ("none" כברירת
  מחדל לקידוחים ישנים) ימשיך לשמש כמטמון עד לביקור הראשון שמדווח ערך.
- **מעבר שדות SVE/BV קבועים למנוע הפרמטרים הגמיש** — בוצע ואומת בשלושת
  המקומות שהוזהרו (טופס ביקור, דשבורד מגמות, ייצוא Excel אינו נוגע
  בשדות שהוסרו כי הוא קורא `visit.recoveryMethod`/שדות שלא הושפעו;
  `sveTrends.ts`/`bioVentingTrends.ts` עודכנו ואומתו מול `tsc`).
- **אין רשת/גישה ל-Firebase מהסביבה הזו** (מגבלה קיימת וידועה כל
  השיחה) — כל שינוי בכללי RTDB או בזרימות שדורשות Auth אמיתי ימשיך
  להיבדק מול Database Emulator מקומי בלבד, לא מול הפרויקט האמיתי. שים
  לב: כתיבת המטמון על ה-`Well` מ-`FuelLensVisitForm.tsx` (עדכון
  `recoveryMethod`/`tankId` לאחר שמירת הביקור) נסמכת על כלל ה-RTDB
  הקיים ל-`wells/$id` (`data.exists() && newData.exists()`), שכבר
  מאפשר לטכנאי לעדכן קידוח קיים — לא נדרש שינוי בכללים.

---

**עודכן לאחר מימוש 4 ההחלטות בסעיף 4 (למעט סעיף 4-4, שעדיין פתוח).
המשך העבודה: פריטי סעיף 5 לפי סדר העדיפות שם.**
